extends layout

block content
  h1 Bem-vindo ao Sistema de Backup
  p Faça upload de um arquivo para realizar o backup.

  form(action="/backup" method="post" enctype="multipart/form-data")
    input(type="file" name="file")
    button(type="submit") Realizar Backup

  h2 Restaurar Arquivo

  form(action="/restore" method="get")
    input(type="text" name="filename" placeholder="Nome do arquivo para restaurar")
    button(type="submit") Restaurar
